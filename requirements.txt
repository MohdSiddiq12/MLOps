# Core libraries
pandas==1.3.3
scikit-learn==0.24.2

# MLflow for experiment tracking
mlflow==1.20.2

# Flask for serving the model
flask==2.0.1

# Gunicorn for deploying the Flask app
gunicorn==20.1.0

# DVC for data version control
dvc==2.5.4

# Additional libraries for logging and monitoring (optional, depending on further needs)
# prometheus_client==0.11.0
# elasticsearch==7.14.0
# opensearch-py==1.0.0
